import 'package:flutter/material.dart';

class SeekerOnboardingScreen extends StatelessWidget {
  const SeekerOnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Seeker Onboarding')),
      body: const Center(child: Text('Seeker Onboarding Placeholder')),
    );
  }
}
